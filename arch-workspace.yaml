# Architecture workspace configuration.
# Run `arch-init` to validate repos and generate .arch/init-state.yaml.

engagement:
  local: engagements/ENG-ARCH-REPO/architecture-repository

enterprise:
  git: { url: "git@github.com-personal:mbauer83/global-architecture-repository.git", branch: main, path: ./enterprise-repository }
