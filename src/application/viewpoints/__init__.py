"""Application-layer viewpoint services: the projection service (companion plan §6) that
produces ``ViewpointProjection`` for both the repository and artifact-local contexts. The
execution use case (companion plan §7) is a later work unit.
"""
