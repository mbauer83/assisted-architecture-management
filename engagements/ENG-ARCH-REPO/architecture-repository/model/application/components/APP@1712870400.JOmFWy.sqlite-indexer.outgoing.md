---
source-entity: APP@1712870400.JOmFWy.sqlite-indexer
version: 0.1.0
status: draft
last-updated: '2026-04-13'
---

<!-- §connections -->

### archimate-access → DOB@1712870400.3rilik.sqlite-index

```yaml
access-type: write
```

The SQLite indexer writes entity and connection metadata to the SQLite index.

### archimate-access → DOB@1712870400.ij5CH7.entity-metadata

```yaml
access-type: write
```

The indexer extracts and stores entity metadata records.

### archimate-access → DOB@1712870400.L3VfsH.connection-metadata

```yaml
access-type: write
```

The indexer extracts and stores connection metadata records.

### archimate-realization → ART@1712870400.FtioFJ.sqlite-database

The SQLite indexer produces the SQLite database artifact.
