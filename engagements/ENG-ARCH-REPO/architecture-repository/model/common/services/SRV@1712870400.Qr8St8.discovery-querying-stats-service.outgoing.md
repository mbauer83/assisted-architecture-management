---
source-entity: SRV@1712870400.Qr8St8.discovery-querying-stats-service
version: 0.1.0
status: draft
last-updated: '2026-04-13'
---

<!-- §connections -->

### archimate-realization → APP@1712870400.v9LvfK.query-engine

The discovery service is realized by the query engine for search and traversal.

### archimate-realization → APP@1712870400.JOmFWy.sqlite-indexer

The discovery service depends on the SQLite indexer for the runtime index.

### archimate-serving → AIF@1712870400.wuleEe.mcp-interface

Discovery operations are exposed through the MCP interface.

### archimate-serving → AIF@1712870400.KxvY-B.cli-interface

Discovery operations are exposed through the CLI interface.

### archimate-realization → REQ@1712870400.Cc2Dd2.semantic-full-text-search

The Discovery Service realizes the semantic full-text search requirement through keyword-scored queries against the SQLite index.

### archimate-realization → REQ@1712870400.F4tfa3.discovery-tools

The Discovery Service directly realizes the discovery tools requirement.

### archimate-realization → REQ@1712870400.s5B3gB.metadata-based-querying

The Discovery Service realizes metadata-based querying via structured AND-filter queries on domain, type, and status fields.

### archimate-realization → REQ@1712870400.eBAa26.graph-based-relationship-discovery

The Discovery Service realizes graph-based relationship discovery via neighbor lookup and multi-hop traversal queries.

### archimate-realization → REQ@1776637159.X5jYC0.concurrent-tool-call-handling
