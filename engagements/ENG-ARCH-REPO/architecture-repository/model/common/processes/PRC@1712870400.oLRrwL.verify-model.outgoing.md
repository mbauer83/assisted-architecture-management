---
source-entity: PRC@1712870400.oLRrwL.verify-model
version: 0.1.0
status: draft
last-updated: '2026-04-13'
---

<!-- §connections -->

### archimate-triggering → FNC@1712870400.ahr0mT.frontmatter-validation

The verify-model process invokes frontmatter validation on every entity, connection file, and diagram.

### archimate-triggering → FNC@1712870400.eAkU8w.referential-integrity-check

The verify-model process invokes referential integrity checks to ensure all references resolve.

### archimate-triggering → FNC@1712870400.wIVwom.puml-syntax-check

The verify-model process invokes PlantUML syntax checking on all diagram files.

### archimate-triggering → FNC@1712870400.B_F-Sq.attribute-schema-validation

The verify-model process invokes attribute schema validation on entity content sections and connection metadata, when per-type attribute schemata are configured.

### archimate-triggering → FNC@1776633078.bPhoR2.frontmatter-parsing

Verify Model invokes Frontmatter Parsing to read and validate all entity frontmatter before schema checks.

### archimate-triggering → EVT@1776633086.yQI1rI.verification-failed

Verify Model raises Verification Failed when the run concludes with one or more errors.

### archimate-realization → SRV@1712870400.Ij6Kl6.validation-service

Verify Model is the internal process realizing the validation service.

### archimate-triggering → PRC@1776633075.vOWkdL.scaffold-diagram
