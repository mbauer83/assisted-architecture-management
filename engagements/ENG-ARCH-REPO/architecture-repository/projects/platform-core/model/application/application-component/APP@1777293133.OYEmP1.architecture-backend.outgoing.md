---
source-entity: APP@1777293133.OYEmP1.architecture-backend
version: 0.1.0
status: draft
last-updated: '2026-07-12'
---

<!-- §connections -->

### archimate-serving → AIF@1712870400.XjbNQh.rest-interface

Architecture Backend exposes the REST Interface by registering FastAPI routers for entities, connections, diagrams, documents, promotion, and sync operations.

### archimate-serving → AIF@1776149386.Ua50xU.web-interface

Architecture Backend serves the compiled Vue SPA as static files, making the Web Interface available to browser clients at the same origin as the REST API.

### archimate-access → DOB@1776633700.I9qEoI.workspace-configuration

Architecture Backend reads the workspace configuration at startup to resolve engagement and enterprise repository root paths for all hosted services.

### archimate-serving → APP@1776149382.lmO0mp.gui-authoring-tool

Architecture Backend provides the REST API and SSE event stream consumed by the GUI Authoring Tool for all model read, write, and real-time update operations.

### archimate-assignment → FNC@1777390460.3ZsfnT.broadcast-model-update

### archimate-serving → APP@1780783708.Ne0utf.architecture-mcp-read-bridge

Architecture Backend serves the Architecture MCP Read Bridge (bridge uses the backend's HTTP API).

### archimate-serving → APP@1780783709.etTj9M.architecture-mcp-write-bridge

Architecture Backend serves the Architecture MCP Write Bridge (bridge uses the backend's HTTP API).

### archimate-serving → APP@1780783710.iu_kKL.assurance-mcp-read-bridge

Architecture Backend serves the Assurance MCP Read Bridge (bridge uses the backend's HTTP API).

### archimate-serving → APP@1780783711.js9xHR.assurance-mcp-write-bridge

Architecture Backend serves the Assurance MCP Write Bridge (bridge uses the backend's HTTP API).

### archimate-access → DOB@1712870400.3rilik.sqlite-index

Architecture Backend reads and writes the SQLite Index for all discovery, querying, and indexing operations.

### archimate-serving → AIF@1782080492.Y4n-FB.assurance-rest-interface

Architecture Backend exposes the Assurance REST Interface, providing HTTP endpoints for assurance analysis lifecycle, node/edge queries, statistics, and diagram rendering.

### archimate-access → DOB@1783266368.4f-C9z.canonical-per-repo-artifact-index

Architecture Backend maintains the canonical per-repo artifact index — building, refreshing, and applying incremental writes to whichever of the (at most two) instances owns the changed physical root.

```yaml
access-type: readwrite
```

### archimate-assignment → FNC@1783266393.4zLFys.compose-combined-scope-read

Architecture Backend performs Compose Combined-Scope Read whenever a caller resolves a combined engagement+enterprise scope.

### archimate-composition → APP@1712870400.JOmFWy.sqlite-indexer

Architecture Backend is composed of the SQLite Indexer as an internal component.

### archimate-composition → APP@1712870400.YOOEbL.frontmatter-parser

Architecture Backend is composed of the Frontmatter Parser as an internal component.

### archimate-composition → APP@1712870400.kRZYOA.architecture-mcp-endpoint-adapter

Architecture Backend hosts the MCP read and write FastMCP server instances, mounting them at /mcp/read and /mcp/write within its HTTP process.

### archimate-composition → APP@1712870400.v9LvfK.query-engine

### archimate-composition → APP@1712870400.yNhgdh.module-catalog

Architecture Backend is composed of the Module Catalog as an internal component.

### archimate-composition → APP@1776633693.tIMxjr.promotion-engine

Architecture Backend hosts the Promotion Engine, invoked via its REST router to execute cross-repository promotion workflows.

### archimate-composition → APP@1776633694.w51oVF.workspace-initializer

Architecture Backend is composed of the Workspace Initializer as an internal component.

### archimate-composition → APP@1776633697.SCKD2U.diagram-scaffolder

Architecture Backend is composed of the Diagram Scaffolder as an internal component.

### archimate-composition → APP@1776637531.a9k_mJ.write-request-queue

Architecture Backend runs the Write Request Queue as an in-process serialization mechanism for all write operations arriving over REST and MCP.

### archimate-composition → APP@1777293134.tY3JIK.git-sync-service

Architecture Backend starts and owns the Git Sync Service on process startup, co-locating filesystem watching and git operations within the same runtime.

### archimate-composition → APP@1777293136.yaxrWl.plantuml-renderer

Architecture Backend is composed of the PlantUML Renderer as an internal component.

### archimate-composition → APP@1777399924.5n_KQJ.bulk-write-handler

### archimate-composition → APP@1777399925.HOOYAQ.batch-transaction-manager

### archimate-composition → APP@1777399925.WK_78v.bulk-delete-handler

### archimate-composition → APP@1777399926.PDJI8F.operation-registry

### archimate-composition → APP@1712870400.ca3vm7.model-verifier

Architecture Backend is composed of the Model Verifier as an internal component.

### archimate-composition → APP@1780656430.m-U5S1.assurance-mcp-endpoint-adapter

Architecture Backend hosts the Assurance MCP Endpoint Adapter's /mcp/assurance-read and /mcp/assurance-write FastMCP server instances, sharing the same process, write queue, and cache.

### archimate-composition → APP@1782080485.uTC8zx.assurance-write-queue

Architecture Backend contains the Assurance Write Queue as an internal concurrency primitive that serializes all assurance store mutations.

### archimate-composition → APP@1782080489.XWVKAX.assurance-read-connection-pool

Architecture Backend contains the Assurance Read Connection Pool, which manages per-thread WAL connections to the confidential assurance store for concurrent reads.

### archimate-composition → APP@1782080499.KqDjXG.unified-assurance-diagram-surface

Architecture Backend is composed of the Unified Assurance Diagram Surface as the GUI subsystem responsible for rendering assurance diagram types from the confidential store.

### archimate-assignment → FNC@1784502389.EhfOy5.resolve-assurance-edge-endpoints

### archimate-assignment → FNC@1784502392.wHsa2g.traverse-assurance-neighbor-graph

### archimate-assignment → FNC@1784502400.xBRahY.serve-assurance-edge-catalog
