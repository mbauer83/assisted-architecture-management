---
source-entity: APP@1712870400.v9LvfK.query-engine
version: 0.1.0
status: draft
last-updated: '2026-04-13'
---

<!-- §connections -->

### archimate-access → DOB@1712870400.3rilik.sqlite-index

```yaml
access-type: read
```

The query engine reads from the SQLite index for search and traversal operations.

### archimate-serving → APP@1776149382.lmO0mp.gui-authoring-tool

The Query Engine provides search, list, and graph-traversal services consumed by the GUI Authoring Tool.

### archimate-access → DOB@1783870961.y7M3wz.viewpoint-definition

Query Engine reads viewpoint definitions to execute their declared scope/selection into an execution representation.

### archimate-composition → APP@1784017465.SZJSV5.relationship-derivation-engine

The Query Engine composes the Relationship Derivation Engine, invoking it to resolve derived-traversal neighbor inclusions, connection selection, and relationship.hops derived attributes during viewpoint execution.

### archimate-composition → APP@1784017469.RyUgp4.query-binding-evaluator

The Query Engine composes the Query Binding Evaluator, invoking it to evaluate a query's let-bindings, parameters, and derived attributes before assembling the primary result.

### archimate-realization → REQ@1712870400.s5B3gB.metadata-based-querying

The query engine fulfils metadata-based querying: it evaluates metadata filters and criteria against the indexed artifact population.
